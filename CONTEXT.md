# LLM Chat

本上下文描述单用户聊天应用中的会话、模型选择和生成记录。

## Language

**会话**：
一组按顺序保存的用户消息和助手回复。

**当前模型**：
会话下一次生成将使用的模型。
_Avoid_: 默认模型、生成模型

**推理档位**：
所有会话共享的当前推理参数原名，取值为 none、low、medium、high、xhigh 或 max。
_Avoid_: 思考模式、中文强度、自动

**推理内容**：
供应商允许展示的推理摘要或 Thinking 内容，不是模型内部思维链。
_Avoid_: 原始思维链、隐藏思考

**生成**：
助手消息的一次具体生成尝试。

**生成快照**：
生成开始时确定且之后不变的模型身份和有效参数记录。
_Avoid_: 当前设置

## Relationships

- 一个 **会话** 有且只有一个 **当前模型**
- 一个 **会话** 包含零个或多个 **生成**
- 每个 **生成** 有且只有一个 **生成快照**
- **推理档位** 只影响之后创建的 **生成**
- **推理内容** 属于一个具体的 **生成**

## Example dialogue

> **开发者：** “用户把推理档位改成 xhigh 后，正在运行的生成要跟着改变吗？”
> **领域专家：** “不要。正在运行的生成保留自己的生成快照，xhigh 只用于之后创建的生成。”

## Flagged ambiguities

- “推理过程”曾同时指供应商公开内容和模型内部思维链；统一使用 **推理内容**，且只表示可公开展示的内容。
- “模型”曾同时指会话选择和历史生成使用的模型；分别使用 **当前模型** 和 **生成快照**。
