# LLM Chat

本上下文定义单用户聊天应用的稳定领域术语。

## Language

**Agent**：可复用的角色设定与执行策略。它包含角色卡、模型默认值、工具权限、Skill 选择和资源额度。会话不归属于 Agent。

**基础系统提示**：属于单个 Agent 的执行配置。角色卡系统提示留空时使用基础提示，`{{original}}` 引用当前 Agent 的基础提示。修改默认 Agent 不影响其他 Agent 的提示。

**默认 Agent**：应用设置中选中的新会话后备 Agent，可以不是内置助手。选择默认 Agent 不复制生成配置；模型、上下文、推理档位与系统提示均在各 Agent 中维护。发送栏仍优先恢复草稿或最近使用的 Agent。

**会话**：一组按顺序保存的用户消息和助手回复。会话可以选择 Agent，也可以绑定一个工作目录。

**有效历史**：当前会话参与显示、上下文、摘要与搜索的消息。旧版撤回形成的隐藏消息仍保留原记录与 ID，不自动回到有效历史。

**离线记录**：当前浏览器保存的服务端会话副本。包含有效历史、生成版本、工具记录和图片，可在断网时搜索和阅读。它不接受离线服务端写入；输入草稿仍由客户端保存。设置开关和占用空间只属于本设备。

**离线同步修订**：会话渲染内容的独立计数，包括流式块、工具输出、用量和附件变化，不以会话更新时间判断内容是否需要重新下载。副本按服务实例标识隔离，清除或退出登录后，旧同步任务不能重新写入。

**分叉**：编辑历史消息及附件，或从历史回复继续时创建的新会话分支。撤回、重做和恢复记录已退役，不再直接回溯当前会话。

**服务目录**：全局搜索引擎和图片工具模型的启用配置与推荐顺序。搜索支持 SearXNG、Tavily，图片模型来自连接配置。AI 可查询目录并选择任意可用项，省略选择时使用首个可用项。推荐顺序不授予工具权限。

**会话工作目录**：会话为后续生成提供的服务端绝对目录。生成开始后保存其规范路径；修改会话工作目录不影响已有生成和后台任务。

**生成**：助手消息的一次具体生成尝试。

**待发送消息**：保存在服务端、归属于一个会话分支的用户消息和附件引用。当前生成结束、失败或取消后，按提交顺序逐条发送，每条启动一次生成。提交时不固定执行快照，实际发送时使用会话的有效配置；可删除尚未开始发送的消息。刷新、换设备和重启不会丢失待发送队列。

**Steer**：待发送消息的一种优先模式。当前模型请求及工具完成后，在下一次模型请求前结束当前生成并优先发送 Steer。消息、工具结果与后续回复各自保留原始记录。它不取消工具或跳过审批，同类消息保持提交顺序；队列暂停时不自动送入。

**快速教程**：可以跳过、关闭和重放的浏览器侧说明。仅观看状态存入当前浏览器的 localStorage，不同步到服务端。它不改变聊天与认证的存储边界。

**取消生成**：终止当前生成及其前台工具，保留已产生的输出。无法及时响应取消的工作在两秒后与生成脱离，迟到结果不能覆盖终态；已启动的独立后台任务继续运行。

**浏览器读取网页**：独立于普通网页读取的可选工具 `browser_fetch`。每次使用隔离的无头浏览器，执行网页脚本并返回文字；Agent 必须显式启用。它不依赖桌面图形环境，限制并发、时长和内容大小，并阻止私网请求。

**生成用量**：一次生成由供应商报告的 token 消耗。它属于生成记录，不表示账户余额。

**账户余额**：供应商账户在某个连接上的当前数值。服务端只在请求时从该连接的同源接口获取，并短暂缓存结果；账户余额不是 Agent 的资源额度。

**生成快照**：生成开始时确定且之后不变的 Agent 版本、模型、有效参数、工具权限、Plugin 修订、Skill 修订和会话工作目录。

**Plugin**：由服务端托管的扩展包。Plugin 只能注册模型可调用的服务端工具。

**Plugin Revision**：Plugin 源内容的不可变、内容寻址副本。

**Plugin Instance**：运行某个 Plugin Revision 的隔离子进程。一个生成始终使用同一组 Plugin Revision。

**工具权限**：Agent 对一个工具的启用状态和审批策略。审批策略为按工具默认、每次审批或自动允许。

**授权工具**：生成快照中已启用且当前可用的工具。只有授权工具可以执行。

**直接工具**：授权工具中从生成第一步开始发送给模型的工具。没有直接性覆盖项的工具默认为直接工具。

**惰性工具**：授权但尚未发送给模型的工具。模型可通过内部 `search_tools` 在当前生成中发现并加载它；加载不改变授权或审批策略。

**Skill**：模型按需加载的工作说明与资源集合。Agent 选择 Skill；生成固定所选 Skill 的修订。

**Agent Skill**：从当前操作系统用户的 `~/.agents/skills` 直接子目录发现的 Skill。内部 ID 为 `agents.<name>`，来源消失后只影响新快照，历史修订仍保留。

**后台任务**：由工具启动并独立于生成生命周期运行的服务端进程。后台任务属于会话，并记录启动它的 Agent 快照。

**当前模型**：会话下一次生成将使用的模型。

**Agent 最近模型**：用户在发送栏明确选择并成功保存到服务端的模型，按 Agent 独立记忆，跨设备共享。它不是 Agent 默认模型，不改变 Agent 修订。新对话没有明确覆盖且 Agent 没有默认模型时，将最近的可用模型固定为会话覆盖。打开历史会话、发送消息和分叉不更新这项记忆；停用或删除的模型不自动继承。

**标签页草稿**：发送栏在当前标签页的 sessionStorage 中保存的文字和已上传附件引用。已有会话按实际分支 ID 隔离；新对话另存 Agent、模型与参数覆盖、工作目录和开场白选择。切页、刷新后恢复，消息被服务端接受或成功入队后清空。当前标签页草稿优先，缺失时使用服务端文字草稿；已有会话继续同步文字到服务端。不同标签页各自编辑，不同步未发送附件。


**推理档位**：Agent 或会话为后续生成选择的推理参数，取值为 none、low、medium、high、xhigh 或 max。

**推理内容**：供应商允许展示的推理摘要或 Thinking 内容，不是模型内部思维链。

## Relationships

- 会话不属于 Agent。会话只为下一次生成选择 Agent。
- 一个生成有且只有一个生成快照。
- 生成用量属于生成；账户余额属于连接，两者没有配额关系。
- 账户余额按需获取，不保存为 Agent 配额。
- 一个生成快照可以引用零个或多个 Plugin Revision 和 Skill Revision。
- 一个后台任务属于一个会话，并可在启动它的生成结束后继续运行。
- 一个 Plugin Instance 运行一个 Plugin Revision，并可由多个固定到该修订的生成租用。
- 工具权限属于 Agent；会话只保留兼容的工具启用覆盖项。
- 搜索凭据和图片工具模型开关属于全局服务目录，不属于 Agent。执行工具时重新读取可用项。
- 图片工具的语义标识持久保存，改名和排序不改变标识。直接 Responses 对话生图不受图片工具开关影响。
- 队列暂停状态独立保存在服务端。旧暂停队列不因升级自动发送，用户手动恢复后继续；暂停期间可以手动发送消息。
- 分叉不回滚工作区文件、Codex thread、记忆或独立后台任务。旧版历史接口不再执行撤回、重做或回溯。
- 工具直接性属于 Agent，不提供会话级覆盖；工具授权、直接性和审批互相独立。
- `search_tools` 只搜索当前生成已经授权的惰性工具，不授予新权限。
- 修改 Agent、工作目录、Plugin 或 Skill 只影响之后创建的快照。

## Flagged Ambiguities

- “任务”必须区分模型生成和后台任务。表示外部进程时始终使用“后台任务”。
- “重新加载 Plugin”表示激活新的修订，不表示修改正在运行生成使用的实例。
- MCP 没有本地 Revision；远程行为和工具元数据都可能在两次生成之间变化。

## Mobile Interaction

手机从屏幕左缘向右滑时，应用按层级返回，不调用浏览器历史返回：先关闭最上层浮层或检查器；后台任务详情返回任务列表，任务列表和运行轨迹返回当前会话；Agent 编辑返回 Agent 列表；聊天、新对话、Agent 列表和设置分区打开导航抽屉。导航抽屉向左滑关闭，选择导航项后收起。桌面不启用返回手势。

纵向滚动交给浏览器。手机主滚动区触顶后允许滚动继续传递到页面，保留 Android 浏览器原生下拉刷新；应用不提供自定义刷新手势或动画。原生刷新前不依赖异步保存，标签页草稿随编辑持续写入。

## Reply Presentation

消息正文是回复区的主体。连续的推理和工具调用合并为处理区段，展开后保留原始顺序；正文、拒绝及其他内容块分隔区段。折叠沿用推理显示偏好，用户手动展开或收起后，流式更新不重置选择。错误、等待审批和停止生成入口直接可见。

完成回复不重复显示头像与身份栏。元信息与操作常驻，桌面优先横排，手机紧凑换行。Agent 身份取自生成或开场白快照，详细用量可在生成检查器中查看。用户消息和对应助手回复均可重试，为同一回答增加版本，保留旧版本。处理区段完成后标题为“推理过程”，展开后直接显示推理正文，不重复标题。

工具展示是原始参数和结果的独立投影。工具可通过 `formatArguments`、`formatResult` 提供缩略和详细 Markdown；折叠时摘要与工具名同行，展开与检查器共享详细展示，并保留原始数据入口。新调用保存展示快照，分叉复制快照，模型上下文仍只接收原始参数和结果。内置历史记录可用纯格式化函数展示，查看旧插件调用不执行插件代码。接入约定见 `docs/TOOL-PRESENTATION.md`。

**富内容预览**：消息中的完整 HTML/CSS/JS 或 SVG 在独立 iframe 中渲染。固定的同源预览页只创建内部沙箱并转发就绪和高度通知，模型内容始终运行在不允许同源访问的内部 iframe 中。主页面 CSP 不放宽脚本执行策略，预览页的独立策略在 200 和 304 响应中保持一致。Service Worker 缓存固定预览页，让自包含文档可离线重开；原始消息不因预览而修改。
