{
  "name": "llm-chat",
  "version": "0.1.0",
  "private": true,
  "license": "Unlicense",
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --filter @llm-chat/web build && concurrently -k -n server,web -c blue,green \"pnpm --filter @llm-chat/server dev\" \"pnpm --filter @llm-chat/web dev\"",
    "build": "pnpm --filter @llm-chat/web build && pnpm --filter @llm-chat/server build",
    "icons:generate": "tsx scripts/generate-icons.ts",
    "start": "pnpm --filter @llm-chat/server start",
    "audit:prod": "pnpm audit --prod",
    "test": "vitest run",
    "test:deploy": "node scripts/deployment-smoke.mjs",
    "test:e2e": "playwright test",
    "test:web:budget": "node scripts/test-web-budget.mjs",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm test:web:budget && pnpm build && pnpm test:coverage"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.6",
    "@types/node": "^24.0.0",
    "@types/proper-lockfile": "^4.1.4",
    "@vitest/coverage-v8": "^3.2.7",
    "concurrently": "^9.2.0",
    "jsdom": "^30.0.1",
    "sharp": "^0.34.5",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
